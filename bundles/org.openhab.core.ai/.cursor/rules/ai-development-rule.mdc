---
description: AI Development Rules for openHAB AI Bundle - Updated based on codebase analysis
globs: src/main/java/org/openhab/core/ai/**/*.java, src/test/java/org/openhab/core/ai/**/*.java
alwaysApply: true
---

# OpenHAB AI Bundle Development Rules

## Core Development Standards

### 1. **Null Safety and Annotations**
- **Use `@NonNullByDefault` on every class** - This is the openHAB standard
- **Add explicit `@Nullable` annotations** for nullable parameters and return types
- **Handle SDK compatibility issues** by adding explicit `@NonNull` annotations where needed for external SDKs
- **Never omit nullability annotations** - always be explicit about null constraints

### 2. **Documentation Standards**
- **Every class must have comprehensive javadoc** with `@author Karel Goderis - Initial Contribution`
- **Include `@since` tags** for version tracking
- **Document all public methods** with parameter descriptions and return value explanations
- **Add class-level documentation** explaining the purpose and responsibilities

### 3. **Implementation Quality**
- **Implement real, functional code** - no stubs or simulated implementations
- **Add TODO comments** for incomplete implementations with clear descriptions
- **Create tasks in BRAIN_PLAN.md** for any incomplete work
- **Verify all method calls exist** before using them - no invented or guessed calls

### 4. **SDK Integration**
- **Maximize use of official SDK classes** for MCP and A2A protocols
- **Use A2A SDK data structures** wherever possible instead of custom implementations
- **Follow SDK patterns** for service registration, lifecycle management, and error handling
- **Verify SDK compatibility** before implementing custom solutions

### 5. **OSGi Container Integration**
- **Use proper OSGi dependency injection** with `@Reference` annotations
- **Implement `@Component` annotations** for service registration
- **Add lifecycle methods** (`@Activate`, `@Deactivate`) for proper bundle management
- **Use `@NonNullByDefault`** with explicit `@Nullable` for OSGi service references

## Architecture and Design Patterns

### 6. **Naming Conventions**
- **Domain-driven naming with clear hierarchy**:
  - `Tool*` prefix for Tool brain core components
  - `AI*` prefix for AI action framework components  
  - `Agent*` prefix for autonomous agent components
  - `Context*` prefix for context and memory systems
  - `Reasoning*` prefix for reasoning and planning components
  - `Event*` prefix for event processing components
  - `Learning*` prefix for learning and feedback systems
  - `Monitor*` prefix for monitoring and optimization
  - No prefix for utility classes and general support

### 7. **Package Structure**
- **Use granular, focused packages** with clear scope separation
- **Avoid broad levels** like "core", "implementation", "infrastructure"
- **Organize by domain** rather than technical layers
- **Follow established package patterns** from the existing codebase

### 8. **Class Organization**
- **Prefer refactoring existing classes** over creating new ones
- **Verify class existence** before creating new classes
- **Use established patterns** from similar existing classes
- **Maintain consistency** with existing naming and structure

## Testing and Quality Assurance

### 9. **Testing Strategy**
- **Create comprehensive unit tests** for all new functionality
- **Use Mockito for dependency mocking** in unit tests
- **Implement integration tests** for service interactions
- **Follow AAA pattern** (Arrange-Act-Assert) for test structure
- **Test both success and failure scenarios**
- **Aim for 80%+ code coverage**

### 10. **Test Organization**
- **Unit tests**: Fast, isolated component tests with mocked dependencies
- **Integration tests**: Component interaction tests with real services
- **Workflow tests**: End-to-end workflow testing
- **Performance tests**: Load testing and performance validation

### 11. **Test Implementation**
- **Use `@ExtendWith(MockitoExtension.class)`** for unit tests
- **Mock openHAB services** (ItemRegistry, ThingRegistry, etc.)
- **Test parameter validation** for all actions
- **Test error handling** and exception scenarios
- **Test async execution** patterns where applicable

## Protocol-Specific Guidelines

### 12. **MCP Protocol Implementation**
- **Use MCP Java SDK** for all MCP-related functionality
- **Follow MCP specification** for tool definitions and responses
- **Implement proper error handling** for MCP protocol errors
- **Use established MCP patterns** from existing code

### 13. **A2A Protocol Implementation**
- **Use A2A Java SDK** for all A2A-related functionality
- **Follow A2A specification** for skill definitions and execution
- **Implement proper task lifecycle** management
- **Use established A2A patterns** from existing code

### 14. **Agent-Skill Architecture**
- **Agents should be agnostic to actions** and only deal with skills
- **Skills encapsulate common actions** shared with MCP implementation
- **Maintain clear separation** between agent logic and action execution
- **Use skill composition** for complex behaviors

## Error Handling and Validation

### 15. **Error Handling**
- **Implement comprehensive error handling** for all public methods
- **Use appropriate exception types** (ActionException, ToolException, etc.)
- **Provide meaningful error messages** with context information
- **Log errors appropriately** with proper log levels

### 16. **Validation**
- **Validate all input parameters** before processing
- **Use ValidationUtils** for common validation patterns
- **Implement parameter schemas** for all actions and tools
- **Provide clear validation error messages**

## Performance and Monitoring

### 17. **Performance Considerations**
- **Use concurrent collections** for thread-safe operations
- **Implement proper resource cleanup** in lifecycle methods
- **Add performance monitoring** with metrics collection
- **Use caching** where appropriate for frequently accessed data

### 18. **Monitoring and Metrics**
- **Add performance metrics** for all major operations
- **Implement health checks** for critical components
- **Use atomic counters** for thread-safe metrics
- **Add proper logging** for debugging and monitoring

## Code Quality and Maintenance

### 19. **Code Organization**
- **Follow single responsibility principle** - each class has one clear purpose
- **Use builder patterns** for complex object construction
- **Implement proper equals/hashCode** for value objects
- **Use immutable objects** where possible

### 20. **Dependency Management**
- **Minimize dependencies** between components
- **Use interfaces** for loose coupling
- **Follow dependency injection** patterns consistently
- **Avoid circular dependencies**

### 21. **Configuration Management**
- **Use configuration services** for all configurable parameters
- **Implement hot-reload** capabilities where appropriate
- **Provide sensible defaults** for all configuration options
- **Validate configuration** on startup

## Security and Safety

### 22. **Security Implementation**
- **Implement proper authentication** for all external interfaces
- **Validate all inputs** to prevent injection attacks
- **Use secure communication** protocols (TLS/SSL)
- **Implement access control** for sensitive operations

### 23. **Safety Constraints**
- **Implement safety checks** for critical operations
- **Add constraint validation** for autonomous behaviors
- **Provide emergency stop** mechanisms where appropriate
- **Log security-relevant events** for audit purposes

## Documentation and Examples

### 24. **Code Documentation**
- **Document complex algorithms** with clear explanations
- **Add usage examples** in javadoc where helpful
- **Document configuration options** with examples
- **Provide troubleshooting information** for common issues
- **openHAB copyright statement** must be added to each class

### 25. **API Documentation**
- **Document all public APIs** with clear examples
- **Provide migration guides** for breaking changes
- **Include performance considerations** in documentation
- **Document error scenarios** and recovery procedures

### 26. **Builder Pattern Best Practices**

- **Immutability-first**
  - Make built types immutable: `private final` fields, `private` constructor, no setters
  - Return defensive, unmodifiable copies for collections (`List.copyOf(...)`, `Set.copyOf(...)`)

- **Null-safety**
  - Annotate all classes with `@NonNullByDefault`
  - Use `@Nullable` for optional fields and builder parameters only
  - Validate non-null/required inputs in `build()` and fail fast with precise messages

- **Structure and naming**
  - Use a nested `public static final class Builder` inside the built type
  - Provide factories: `public static Builder builder(RequiredA a, RequiredB b)` or `builder()` with `withRequiredX(...)`
  - Use `withX(...)` fluent setters; each returns `this`

- **Required vs optional**
  - Capture required fields early (factory or builder constructor) and re-validate in `build()`
  - Avoid encoding “required” as nullable; prefer types that enforce presence

- **Validation**
  - Validate ranges, formats, and cross-field constraints in `build()`
  - Throw `IllegalArgumentException` with actionable messages; use `ValidationUtils` where applicable

- **Collections**
  - Accept collections in the builder; normalize and copy in `build()`
  - Store as unmodifiable; never expose internal mutables

- **Copy/modify ergonomics**
  - Provide `toBuilder()` or `builder(existing)` to support small mutations without exposing mutability

- **Thread-safety**
  - Builders are not thread-safe (document as such); built objects are thread-safe by immutability

- **Equality and debugging**
  - Implement `equals`, `hashCode`, and `toString` on the built type (not on the builder)

- **Usage discipline**
  - Treat builders as single-use; keep `build()` side-effect free (construct, validate, copy)

- **When to use**
  - Prefer builders for objects with many parameters, optional fields, or cross-field validation
  - For simple data types (≤2–3 params), use constructors or static factories

- **Inheritance**
  - Prefer composition for value objects; if inheritance is necessary, use self-referential generics for covariant builder returns

- **Documentation**
  - Add class-level Javadoc (purpose, constraints, usage) and method docs per standards (author/since)

```java
// Example (openHAB style): immutable value with nested builder
package org.openhab.core.ai.example;

import org.eclipse.jdt.annotation.NonNullByDefault;
import org.eclipse.jdt.annotation.Nullable;

import java.util.List;
import java.util.Objects;

@NonNullByDefault
public final class ModelConfig {
    private final String modelId;
    private final int maxTokens;
    private final List<String> fallbackModels;
    private final @Nullable String providerHint;

    private ModelConfig(Builder b) {
        this.modelId = b.modelId;
        this.maxTokens = b.maxTokens;
        this.fallbackModels = List.copyOf(b.fallbackModels);
        this.providerHint = b.providerHint;
    }

    public static Builder builder(String modelId) {
        return new Builder(modelId);
    }

    public Builder toBuilder() {
        return new Builder(this);
    }

    public static final class Builder {
        private String modelId;
        private int maxTokens = 2048;
        private List<String> fallbackModels = List.of();
        private @Nullable String providerHint;

        public Builder(String modelId) {
            this.modelId = Objects.requireNonNull(modelId, "modelId");
        }

        public Builder(ModelConfig source) {
            this.modelId = source.modelId;
            this.maxTokens = source.maxTokens;
            this.fallbackModels = source.fallbackModels;
            this.providerHint = source.providerHint;
        }

        public Builder withMaxTokens(int maxTokens) {
            this.maxTokens = maxTokens;
            return this;
        }

        public Builder withFallbackModels(List<String> fallbackModels) {
            this.fallbackModels = Objects.requireNonNull(fallbackModels, "fallbackModels");
            return this;
        }

        public Builder withProviderHint(@Nullable String providerHint) {
            this.providerHint = providerHint;
            return this;
        }

        public ModelConfig build() {
            if (modelId.isBlank()) {
                throw new IllegalArgumentException("modelId must not be blank");
            }
            if (maxTokens <= 0) {
                throw new IllegalArgumentException("maxTokens must be > 0");
            }
            return new ModelConfig(this);
        }
    }
}
```

### 27. **Result Classes Best Practices**

- **Immutability and null-safety**
  - Use immutable value objects: `private final` fields, no setters, defensive copies
  - Annotate classes with `@NonNullByDefault`; use `@Nullable` only for truly optional fields
  - Return empty collections instead of `null`; avoid `Optional` in getters in favor of `@Nullable` + docs

- **Consistent shape**
  - Include a status enum (e.g., `SUCCESS`, `FAILURE`, `PARTIAL`) and helpers like `isSuccess()`
  - Prefer machine-readable fields (enums/typed codes) over free-form text
  - Separate payload from metadata (status, errors, metrics)

- **Errors and diagnostics**
  - Provide `errorCode` (enum), human `message`, optional structured `details`/`hint`
  - Add traceability: `correlationId`/`requestId`, optional `source`
  - Store safe `causeMessage` strings; avoid retaining `Throwable` instances

- **Timing and metrics**
  - Include `timestamp` and optional `durationMs` when representing operations
  - Keep metrics optional and non-intrusive

- **Collections and serialization**
  - Store unmodifiable copies (`List.copyOf(...)`, `Set.copyOf(...)`); never expose mutables
  - Keep fields REST/JSON friendly (simple types, enums); avoid transient internals

- **Equality and debugging**
  - Implement `equals`, `hashCode`, and concise `toString` (omit sensitive/large fields)
  - Define equality semantics (e.g., exclude metrics) and keep consistent

- **Construction API**
  - Provide static factories like `success(...)`, `failure(...)`, `partial(...)`
  - Use a nested `Builder` only when parameters are many or cross-field validation is complex

- **No side effects**
  - Result types should not log or perform I/O; log at call sites

- **Extensibility**
  - For families of results, use an interface or sealed hierarchy for shared fields; prefer composition otherwise

- **Testing**
  - Test success/failure/partial cases, equality contract, and (if applicable) JSON round-trip

```java
package org.openhab.core.ai.result;

import org.eclipse.jdt.annotation.NonNullByDefault;
import org.eclipse.jdt.annotation.Nullable;

import java.time.Instant;
import java.util.List;
import java.util.Objects;

@NonNullByDefault
public final class OperationResult<T> {
    public enum Status { SUCCESS, FAILURE, PARTIAL }
    public enum ErrorCode { NONE, VALIDATION, AUTH, TIMEOUT, INTERNAL }

    private final Status status;
    private final @Nullable T payload;
    private final ErrorCode errorCode;
    private final String message;
    private final List<String> warnings;
    private final String correlationId;
    private final Instant timestamp;
    private final long durationMs;

    private OperationResult(
            Status status,
            @Nullable T payload,
            ErrorCode errorCode,
            String message,
            List<String> warnings,
            String correlationId,
            Instant timestamp,
            long durationMs) {
        this.status = Objects.requireNonNull(status);
        this.payload = payload;
        this.errorCode = Objects.requireNonNull(errorCode);
        this.message = Objects.requireNonNull(message);
        this.warnings = List.copyOf(warnings);
        this.correlationId = Objects.requireNonNull(correlationId);
        this.timestamp = Objects.requireNonNull(timestamp);
        this.durationMs = durationMs;
    }

    public static <T> OperationResult<T> success(T payload, String correlationId, long durationMs) {
        return new OperationResult<>(Status.SUCCESS, payload, ErrorCode.NONE, "", List.of(),
                correlationId, Instant.now(), durationMs);
    }

    public static <T> OperationResult<T> failure(ErrorCode code, String message, String correlationId, long durationMs) {
        return new OperationResult<>(Status.FAILURE, null, code, message, List.of(),
                correlationId, Instant.now(), durationMs);
    }

    public static <T> OperationResult<T> partial(T payload, List<String> warnings, String correlationId, long durationMs) {
        return new OperationResult<>(Status.PARTIAL, payload, ErrorCode.NONE, "", warnings,
                correlationId, Instant.now(), durationMs);
    }

    public boolean isSuccess() { return status == Status.SUCCESS; }
    public boolean isPartial() { return status == Status.PARTIAL; }

    // getters ...
}
```
